"use client";
import{useState}from"react";
export default function CompanyMemberInviteForm(){const[message,setMessage]=useState<string|null>(null),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage(null);const f=new FormData(e.currentTarget);const r=await fetch("/api/ai-workspace/company/invite",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email"),membershipRole:f.get("membershipRole"),roleLabel:f.get("roleLabel")})});const j=await r.json();setMessage(r.ok?(j.status==="active"?"Member access activated. Company wallet will pay for assigned AI usage.":"Invitation sent. Access activates only after the invited email is verified."):(j.error||"Invitation failed"));if(r.ok)e.currentTarget.reset();setBusy(false);}return <form onSubmit={submit} style={{display:"grid",gap:10,maxWidth:620}}><input name="email" type="email" required placeholder="user@example.com"/><input name="roleLabel" required defaultValue="Developer" placeholder="Role label"/><select name="membershipRole" defaultValue="operator"><option value="operator">Operator</option><option value="executive">Executive</option><option value="auditor">Auditor</option><option value="viewer">Viewer</option></select><button disabled={busy} type="submit">{busy?"Inviting…":"Invite member"}</button>{message?<p role="status">{message}</p>:null}</form>;}
