import Link from "next/link";
import {requireOwnerOrganizationContext} from "@/lib/auth/organization-context";
import {stripeConfigured} from "@/lib/billing/stripe-rest";
export const dynamic="force-dynamic";
const money=(n:number,c:string)=>new Intl.NumberFormat("en-IE",{style:"currency",currency:c||"EUR"}).format(n||0);
export default async function BillingPage(){const {supabase,organizationId,organization}=await requireOwnerOrganizationContext();const [accountR,subsR,payR,offersR,agentsR]=await Promise.all([supabase.from("billing_accounts").select("*").eq("organization_id",organizationId).maybeSingle(),supabase.from("billing_subscriptions").select("*").eq("organization_id",organizationId).order("created_at",{ascending:false}),supabase.from("billing_payments").select("*").eq("organization_id",organizationId).order("created_at",{ascending:false}).limit(20),supabase.from("commercial_offers").select("offer_code,name,summary,currency,base_price,billing_interval,self_serve,status").eq("status","public").eq("self_serve",true).order("sort_order"),supabase.from("agents").select("id,name,sale_price_monthly,cost_currency,cost_model,agent_status").eq("organization_id",organizationId).neq("agent_status","archived")]);const account=accountR.data;const subscriptions=subsR.data??[];const payments=payR.data??[];const offers=offersR.data??[];const agents=agentsR.data??[];const active=subscriptions.find((s:any)=>["active","trialing","past_due"].includes(s.status));const recurringAgents=agents.reduce((s:number,a:any)=>s+Number(a.sale_price_monthly??0),0);return <main className="command-shell finance-shell"><header className="command-header"><div><p className="eyebrow">RYTHM BILLING</p><h1>Billing & subscription</h1><p className="subtitle">Independent billing identity for {organization.name}. Platform subscription, AI workforce charges, payments and invoices remain tenant-isolated.</p></div><Link className="secondary-button" href="/finance">Finance Center</Link></header><section className="finance-kpis"><article><span>Subscription</span><strong>{active?.status??"Not active"}</strong><small>{active?.current_period_end?`Renews ${new Date(active.current_period_end).toLocaleDateString("en-GB")}`:"No active billing period"}</small></article><article><span>Stripe customer</span><strong>{account?.provider_customer_id?"Connected":"Pending"}</strong><small>{account?.billing_email??"Created on first checkout"}</small></article><article><span>AI positions / month</span><strong>{money(recurringAgents,organization.default_currency??"EUR")}</strong><small>Recurring position value configured in Workforce</small></article><article><span>Payment rail</span><strong>{stripeConfigured()?"Ready":"Configuration required"}</strong><small>RYTHM is merchant of record; companies are customers</small></article></section><section className="finance-grid"><article className="finance-panel"><div className="finance-panel-head"><div><p className="eyebrow">PLATFORM PLANS</p><h2>Company subscription</h2></div></div><div className="finance-list">{offers.map((o:any)=><div className="finance-row" key={o.offer_code}><div><strong>{o.name}</strong><small>{o.summary}</small></div><div><strong>{o.base_price!=null?money(Number(o.base_price),o.currency??"EUR"):"Custom"}</strong><small>{o.billing_interval}</small></div><form action="/api/billing/checkout" method="POST"><input type="hidden" name="offerCode" value={o.offer_code}/><button type="submit" disabled={!stripeConfigured()||Boolean(active)}>Subscribe</button></form></div>)}</div></article><article className="finance-panel"><div className="finance-panel-head"><div><p className="eyebrow">SELF SERVICE</p><h2>Customer billing portal</h2></div></div><p>Owners can update payment methods, view Stripe invoices, and manage cancellation through a tenant-bound portal session.</p><form action="/api/billing/portal" method="POST"><button type="submit" disabled={!stripeConfigured()||!account?.provider_customer_id}>Open billing portal</button></form><p className="finance-note">Cancellation defaults to end-of-period. Entitlements must be changed only from verified webhook state, never from a browser redirect.</p></article></section><section className="finance-grid"><article className="finance-panel"><div className="finance-panel-head"><div><p className="eyebrow">PAYMENTS</p><h2>Recent provider payments</h2></div></div><div className="finance-list">{payments.length?payments.map((p:any)=><div className="finance-row" key={p.id}><div><strong>{p.status}</strong><small>{p.provider_invoice_id??p.provider_payment_intent_id??"Stripe"}</small></div><div><strong>{money(Number(p.amount),p.currency)}</strong><small>{new Date(p.created_at).toLocaleDateString("en-GB")}</small></div></div>):<p>No provider payments recorded yet.</p>}</div></article><article className="finance-panel"><div className="finance-panel-head"><div><p className="eyebrow">TENANT BOUNDARY</p><h2>Independent company infrastructure</h2></div></div><ul><li>One Stripe Customer mapping per RYTHM organization.</li><li>Subscriptions, line items, payments and checkout sessions carry organization identity.</li><li>AI positions can become recurring subscription line items independently of the platform plan.</li><li>Webhook events are idempotent and service-only.</li><li>Finance Center receives provider invoice/payment references without becoming the statutory invoice issuer.</li></ul></article></section></main>}
