# Evaluation Runtime Note

The 8-Agent benchmark executes Agent scenarios concurrently so the owner-triggered evaluation remains within serverless runtime limits. Each scenario still produces isolated evidence and a separate judge result; governance boundaries are unchanged.
